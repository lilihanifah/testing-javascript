const array = ["A", "B", "C", "D", "E"];

array.forEach((item,idx)=> {
    console.log(`Nomor ${idx}, Nama ${item}`);
});

console.log()

const array1 = ["1", "2", "3", "4", "5", "6", "7", "8"];

array1.forEach((element)=>
    console.log(element));